# ── Frontend (Vite) — copy these into frontend/.env or frontend/.env.local ────
# These are exposed to the browser; use anon key only, never service_role.
VITE_SUPABASE_URL=      # same Project URL as SUPABASE_URL above
VITE_SUPABASE_ANON_KEY= # same anon key as SUPABASE_ANON_KEY above
