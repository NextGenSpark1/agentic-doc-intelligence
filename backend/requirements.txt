# --- API ---
fastapi
uvicorn[standard]
python-multipart        # multipart file uploads
pydantic
pydantic-settings
python-dotenv

# --- Storage / DB (Postgres + Storage + pgvector, managed) ---
supabase

# --- AI layer ---
agentic-doc             # LandingAI ADE (isolated in ade_client.py; migrate to landingai-ade later)
litellm                 # provider-agnostic LLM + embeddings router
