# --- API ---
fastapi
uvicorn[standard]
python-multipart        # multipart file uploads
pydantic
pydantic-settings
python-dotenv

# --- Storage / DB (Postgres + Storage + pgvector, managed) ---
supabase

# --- AI layer ---
landingai-ade           # LandingAI ADE — replaces deprecated agentic-doc
litellm                 # provider-agnostic LLM + embeddings router
